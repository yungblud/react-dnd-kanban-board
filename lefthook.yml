pre-commit:
  parallel: true
  jobs:
    - run: npm run eslint {staged_files}
      glob: '*.{js,ts,jsx,tsx}'
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
